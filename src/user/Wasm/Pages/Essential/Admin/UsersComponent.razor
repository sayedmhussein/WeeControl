@using WeeControl.User.UserApplication.ViewModels.Admin
@inject ListOfUsersViewModel ViewModel

<p>List of users</p>
<button class="btn btn-primary" @onclick="@ViewModel.GetListOfUsers" disabled="@ViewModel.IsLoading">Refresh</button>

@if (ViewModel.IsLoading)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table table-bordered table-hover">
        <thead>
        <tr>
            <th scope="col">Username</th>
            <th scope="col">Email</th>
            <th scope="col">Territory</th>>
        </tr>
        </thead>
        <tbody>
        @foreach (var t in ViewModel.ListOfUsers)
        {
            <tr>
                <td>@t.Username</td>
                <td>@t.Email</td>
                <td>@t.TerritoryCode</td>
            </tr>
        }
        </tbody>
    </table>
}



@code {
    protected override async Task OnInitializedAsync()
    {
        await ViewModel.GetListOfUsers();
        await base.OnInitializedAsync();
    }
}