@page "/ForgotPassword"
@page "/Authentication/ForgotPassword"
@using WeeControl.SharedKernel.Essential
@using WeeControl.Presentations.Wasm.Shared
@using WeeControl.SharedKernel.Essential.DataTransferObjects
@layout RestrictedLayout

@inject NavigationManager NavManager
@inject IUserOperation UserOperation
@attribute [AllowAnonymous]

<div class="card">
    <h4 class="card-header">Please enter your username and and email address</h4>
    <div class="card-body">
        <EditForm Model="@resetPasswordResetRequestDto" OnValidSubmit="RequestPasswordReset">
            <DataAnnotationsValidator/>
            <div class="form-group">
                <label>Email</label>
                <InputText @bind-Value="resetPasswordResetRequestDto.Email" type="email" class="form-control"/>
                <ValidationMessage For="@(() => resetPasswordResetRequestDto.Email)"/>
            </div>
            <div class="form-group">
                <label>Username</label>
                <InputText @bind-Value="resetPasswordResetRequestDto.Username" class="form-control"/>
                <ValidationMessage For="@(() => resetPasswordResetRequestDto.Username)"/>
            </div>
            <button class="btn btn-primary">
                @if (loading)
                {
                    <span class="spinner-border spinner-border-sm mr-1"></span>
                }
                Request New Password
            </button>
            @if (!string.IsNullOrEmpty(stringAlert))
            {
                <div class="alert alert-danger mt-3 mb-0">@stringAlert</div>
            }
        </EditForm>
    </div>
</div>

@code
{
    private PasswordResetRequestDto resetPasswordResetRequestDto = new();
    private bool loading;
    private string stringAlert;

    private async Task RequestPasswordReset()
    {
        loading = true;
        await UserOperation.ForgotPasswordAsync(resetPasswordResetRequestDto);
        NavManager.NavigateTo("/login");
    }
}