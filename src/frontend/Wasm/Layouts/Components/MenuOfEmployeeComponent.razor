



@foreach (var b in UserService.MenuItems)
{
    <MudMenuItem>
        <MudButton 
            StartIcon="@Icons.Material.Outlined.Computer"
            Href="@b.PageName">@b.PageName
        </MudButton>
    </MudMenuItem>
}

@code {
    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        await UserService.Sync();
    }
}