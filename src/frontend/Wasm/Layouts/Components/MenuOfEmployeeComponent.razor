@using WeeControl.Frontend.ApplicationService.Contexts.Essential.Interfaces
@inject IHomeViewModel HomeViewModel

@foreach (var b in HomeViewModel.MenuItems)
{
    <MudMenuItem>
        <MudButton 
            StartIcon="@Icons.Material.Outlined.Computer"
            Href="@b.PageName">@b.PageName
        </MudButton>
    </MudMenuItem>
}

@code {
    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        await HomeViewModel.Refresh();
    }
}